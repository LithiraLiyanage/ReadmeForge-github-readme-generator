import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{toast}from"react-toastify";import{useAuth}from"../context/AuthContext";import{passwordStrong}from"../utils/validators";const Register=()=>{const{register}=useAuth();const nav=useNavigate();const[form,setForm]=useState({name:"",email:"",password:"",confirm:""});const submit=async e=>{e.preventDefault();if(form.name.length<2)return toast.error("Name must be at least 2 characters");if(!passwordStrong(form.password))return toast.error("Password must include uppercase lowercase number and 8+ chars");if(form.password!==form.confirm)return toast.error("Passwords do not match");try{const path=await register({name:form.name,email:form.email,password:form.password});toast.success("Account created");nav(path)}catch(err){toast.error(err.response?.data?.message||"Registration failed")}};return <main className="flex min-h-[80vh] items-center justify-center px-4 py-12"><form onSubmit={submit} className="card-green w-full max-w-lg p-8"><h1 className="text-3xl font-black">Create your account</h1><input className="input mt-6" placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input mt-4" type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input mt-4" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><input className="input mt-4" type="password" placeholder="Confirm password" value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})}/><button className="btn-primary mt-6 w-full">Register</button><p className="mt-5 text-center text-sm">Already have an account? <Link className="font-bold text-primary" to="/login">Login</Link></p></form></main>};export default Register;
