import{useEffect,useState}from"react";import{toast}from"react-toastify";import api from"../services/api";import ReadmeCard from"../components/ReadmeCard";import EmptyState from"../components/EmptyState";const SavedReadmes=()=>{const[items,setItems]=useState([]),[search,setSearch]=useState("");const load=()=>api.get(`/readmes?search=${encodeURIComponent(search)}`).then(r=>setItems(r.data));useEffect(()=>{load()},[]);const favorite=async id=>{await api.put(`/readmes/${id}/favorite`);toast.success("Favorite updated");load()};const remove=async id=>{if(confirm("Delete README?")){await api.delete(`/readmes/${id}`);toast.success("Deleted");load()}};return <div><div className="flex flex-wrap items-center justify-between gap-3"><h1 className="text-3xl font-black">Saved READMEs</h1><div className="flex gap-2"><input className="input" placeholder="Search..." value={search} onChange={e=>setSearch(e.target.value)}/><button className="btn-primary" onClick={load}>Search</button></div></div><div className="mt-6 grid gap-5 md:grid-cols-3">{items.length?items.map(x=><ReadmeCard key={x._id} item={x} onFavorite={favorite} onDelete={remove}/>):<div className="md:col-span-3"><EmptyState title="No READMEs saved" text="Generate and save your first README."/></div>}</div></div>};export default SavedReadmes;
