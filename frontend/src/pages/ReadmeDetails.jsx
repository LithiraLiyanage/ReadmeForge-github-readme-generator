import{useEffect,useState}from"react";import{useParams}from"react-router-dom";import api from"../services/api";import MarkdownPreview from"../components/MarkdownPreview";import CopyButton from"../components/CopyButton";import DownloadButton from"../components/DownloadButton";const ReadmeDetails=()=>{const{id}=useParams();const[item,setItem]=useState(null),[mode,setMode]=useState("preview");useEffect(()=>{api.get(`/readmes/${id}`).then(r=>setItem(r.data))},[id]);if(!item)return"Loading...";return <div><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-3xl font-black">{item.projectName}</h1><p className="text-slate-500">{item.category} · {item.styleOption}</p></div><div className="flex gap-2"><button className="btn-secondary" onClick={()=>setMode(mode==="preview"?"markdown":"preview")}>{mode==="preview"?"Markdown":"Preview"}</button><CopyButton text={item.generatedMarkdown}/><DownloadButton markdown={item.generatedMarkdown}/></div></div><div className="mt-6"><MarkdownPreview markdown={item.generatedMarkdown} mode={mode}/></div></div>};export default ReadmeDetails;
