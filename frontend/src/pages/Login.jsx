import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{toast}from"react-toastify";import{useAuth}from"../context/AuthContext";const Login=()=>{const{login}=useAuth();const nav=useNavigate();const[form,setForm]=useState({email:"",password:""});const submit=async e=>{e.preventDefault();try{const path=await login(form.email,form.password);toast.success("Login successful");nav(path)}catch(err){toast.error(err.response?.data?.message||"Login failed")}};return <main className="flex min-h-[80vh] items-center justify-center px-4 py-12"><form onSubmit={submit} className="card-green w-full max-w-md p-8"><h1 className="text-3xl font-black">Welcome back</h1><p className="mt-2 text-slate-500">Login to your ReadmeForge workspace.</p><input className="input mt-6" type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input mt-4" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn-primary mt-6 w-full">Login</button><p className="mt-5 text-center text-sm">No account? <Link className="font-bold text-primary" to="/register">Register</Link></p><p className="mt-4 rounded-xl bg-green-50 p-3 text-xs text-slate-500">Admin: admin@example.com / Admin12345</p></form></main>};export default Login;
