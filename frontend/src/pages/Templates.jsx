import{useEffect,useState}from"react";import api from"../services/api";import TemplateCard from"../components/TemplateCard";const Templates=()=>{const[templates,setTemplates]=useState([]);useEffect(()=>{api.get("/templates").then(r=>setTemplates(r.data))},[]);return <main className="mx-auto max-w-7xl px-4 py-16"><h1 className="text-4xl font-black">README Templates</h1><p className="mt-3 text-slate-500">Start from a professional template and customize your README.</p><div className="mt-8 grid gap-5 md:grid-cols-3">{templates.map(t=><TemplateCard key={t._id} template={t}/>)}</div></main>};export default Templates;
