import{techOptions}from"../utils/badgeMap";const TechStackSelector=({value=[],onChange})=>{const toggle=tech=>value.includes(tech)?onChange(value.filter(x=>x!==tech)):value.length<20&&onChange([...value,tech]);return <div><p className="mb-3 text-sm font-bold text-slate-700">Tech Stack</p><div className="flex flex-wrap gap-2">{techOptions.map(tech=><button type="button" key={tech} onClick={()=>toggle(tech)} className={`rounded-full border px-3 py-2 text-sm font-bold transition ${value.includes(tech)?"border-primary bg-green-100 text-primary":"border-green-100 bg-white text-slate-600 hover:bg-green-50"}`}>{tech}</button>)}</div></div>};export default TechStackSelector;
